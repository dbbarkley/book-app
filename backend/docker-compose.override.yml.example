# Copy this file to docker-compose.override.yml to customize your local setup
# docker-compose.override.yml is automatically loaded by docker-compose

version: '3.8'

services:
  web:
    # Uncomment to run commands manually
    # command: bash
    # Uncomment to expose additional ports
    # ports:
    #   - "3001:3001"
    environment:
      # Override environment variables here
      # - RAILS_ENV=development

